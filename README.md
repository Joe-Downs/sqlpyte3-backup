# database
The `database` directory holds all scripts and functions that allow the bot and website to interact with the database of users. Function definitions for any and all calls to the SQLite3 database can be found in this directory.

Some functions found here:
- setRank()
- getRankFromName()
- getNameFromRank()
- getTopNumRanks()